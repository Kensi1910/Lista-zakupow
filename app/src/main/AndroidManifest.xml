<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddListActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".FormulaAddNewCategoryActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".AddProduktTabLayout" />
        <activity
            android:name=".AddCategory"
            android:label="Food List"
            android:parentActivityName=".CategoryActivity" />
        <activity android:name=".ChangePictureCategoryActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".przepisy_activity" />
        <activity android:name=".PrzepisyDetailActivity" />

        <receiver
            android:name=".AlarmReceiver"
            android:process=":remote" />

        <activity android:name=".SetReminderWindow" />
        <activity android:name=".AutorzyActivity"></activity>
    </application>

</manifest>